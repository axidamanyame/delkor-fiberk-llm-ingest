# Delkor-Fiberk — KorBek AI workspace

You are in the **KorBek AI** project (one of three). Read first:

1. `PROJECTS.md`
2. `LOCKSTEP.md`
3. `WHAT-WE-BUILT.md`
4. `ERP-CONSTITUTION.md` / `SYSTEM-PROMPT.md`
5. `catalog/SUPPLY-CHAIN-CATALOG-MASTER-TRAINING.md`

## This workspace is the in-house model

- One brain for the group. You **know** the ERP, the Catalog, and the BNPL field apps. You do **not** merge them.
- Load this lock-step pack as knowledge. Do not paste markdown into the founder’s chat and call that “training.”
- Live ERP writes are **not connected**. Propose and log. Never claim a remote apply. Never hold API keys.
- Default later runtime (when the founder opens it): Electron + Ollama, model `qwen2.5-coder:7b`. Web desk is optional.

## Do not send the founder to ollama-electron

That folder is **not** this sitting’s build. The founder attached this zip so **you** are up to speed.

- Do not ask them to copy files to `C:\Users\delko\ollama-electron`.
- Do not produce a “PUT-ON-PC” hunt list.
- When they later open the KorBek desktop folder and ask you to install knowledge, **you** write the files. Until then, this pack is the knowledge.

## Smoke tests (when the model actually runs)

1. “Using your training document, what does Avenue see for FBK1829?” → **A1**
2. “What products are we building?” → ERP, Catalog, KorBek (and BNPL field apps / ingest as related) — **not one app**.

## Do not mix

- ERP stays vanilla HTML/JS. Catalog photos stay out of the ERP. Stall code is A1.
- BNPL FieldSales.PWA is a separate product from ERP Collections.
- Private GitHub `eve-chat-template` and `star-atlas-berry-stone` are not group ops.

## Founder time

Produce the drop. Do not send a hunt list. No secrets in public packs.
